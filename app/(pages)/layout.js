import React from "react";
import "../globals.css";

import Sidebar from "@/components/Sidebar";

export const metadata = {
  title: "CLC - Children Learning Center",
  description: "Generated by SweatBox app",
};

const LayoutPage = ({ children }) => {
  return (
    <div className="flex overflow-auto max-h-screen">
      <Sidebar />

      <div className="w-full min-h-screen rounded-lg overflow-auto bg-blue-50 pb-10">
        {children}
      </div>
    </div>
  );
};

export default LayoutPage;
